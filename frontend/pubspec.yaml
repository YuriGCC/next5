name: frontend
description: "Next5 app"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.2
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  # gallery_saver: ^2.3.2
  dio: ^5.9.0
  google_fonts: ^6.3.1
  flutter_secure_storage: ^9.2.4
  provider: ^6.1.5
  flutter_ffmpeg: ^0.4.0
  video_trimmer: ^0.4.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

